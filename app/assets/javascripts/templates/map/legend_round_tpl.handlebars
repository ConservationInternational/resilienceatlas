{{#if units}} <span class="units">{{units}}</span> {{/if}}
<div class="m-legend__round">
  {{#each data}}
    <div class="item"><span style="background-color: {{this.value}}"></span> {{this.name}}</div>
  {{/each}}
</div>
