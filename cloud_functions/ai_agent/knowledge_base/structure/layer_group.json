{
  "id": "doc::structure::layer_group",
  "title": "LayerGroup — Layer Organization and Navigation",
  "description": "LayerGroups organize layers into hierarchical categories shown in the left panel. Each group belongs to a site_scope and may have a parent (super_group). Layers are linked to groups via Agrupation join records.",
  "key_fields": {
    "id": "Integer primary key.",
    "name": "Display name shown in the sidebar (translated).",
    "slug": "URL-safe identifier for the group, unique per site_scope.",
    "site_scope_id": "Foreign key to SiteScope. Required.",
    "super_group_id": "Optional. Foreign key to parent LayerGroup for nesting.",
    "layer_group_type": "Controls display mode. Common values: 'radio' (only one active at a time), 'checkbox' (multiple active), null (default folder).",
    "category": "Optional grouping category label for the group.",
    "active": "Boolean. Default active state on page load.",
    "order": "Integer display order within the parent group.",
    "icon_class": "Optional CSS icon class for the group icon."
  },
  "linking_layers": {
    "description": "When creating a layer, pass site_scope_id and layer_group_id. The admin API creates the Agrupation automatically.",
    "endpoint": "POST /api/admin/layers with body: {\"site_scope_id\": <id>, \"layer\": {\"layer_group_id\": <group_id>, ...other fields}}"
  },
  "how_to_find_groups": "Use GET /api/admin/layers which returns the full list. Or use the admin UI. Layer group IDs are visible in the URL when editing in ActiveAdmin.",
  "usage_notes": [
    "A layer can belong to multiple layer groups (via multiple Agrupation records), but this is uncommon.",
    "If the correct layer group does not exist, one must be created in ActiveAdmin first.",
    "The super_group_id creates nesting — sub-groups appear nested under their parent in the sidebar.",
    "layer_group_type 'radio' is common for basemap-style groups where only one layer should be active."
  ]
}
